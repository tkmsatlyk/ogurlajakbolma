name: Fetch and Clean Free VPN Links
on:
  schedule:
    - cron: '*/30 * * * *' # Her 30 dakikada bir en taze düşenleri yakalar
  workflow_dispatch:

jobs:
  fetch-links:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Fetch and Filter Port 443 and 80 Links
        run: |
          # Eski listeyi sıfırla
          > toplanan_linkler.txt
          touch temp_live.txt

          # Canlı ve devasa proxy havuzları
          URLS=(
            "https://raw.githubusercontent.com/Zu6666/v2ray-share/main/all_links.txt"
            "https://raw.githubusercontent.com/yebekhe/TVC/main/v2ray/mix.txt"
            "https://raw.githubusercontent.com/mahdibland/V2RayAggregator/master/sub/sub_merge.txt"
            "https://raw.githubusercontent.com/awesome-vpn/vpn/master/v2ray"
          )

          for url in "${URLS[@]}"; do
            curl -sL --fail --max-time 15 "$url" > single_raw.txt || continue
            
            if [ -s single_raw.txt ] && ! grep -q "Not Found" single_raw.txt; then
              if cat single_raw.txt | base64 -d > single_decoded.txt 2>/dev/null; then
                cat single_decoded.txt >> temp_live.txt
              else
                cat single_raw.txt >> temp_live.txt
              fi
            fi
          done

          # 1. Aşama: Geçerli protokol satırlarını süz
          grep -iE "(vless|vmess|trojan|ss|ssr|hysteria|tuic):\/\/" temp_live.txt > filtered_proto.txt || true
          
          # 2. Aşama: Regex ile Sadece :443 ve :80 portuna sahip olan linkleri cımbızla çek
          # Bu komut linkin içindeki IP veya Domain'den sonra gelen ":443" veya ":80" kalıbını arar
          grep -E "(:443|:80)[^0-9]" filtered_proto.txt > filtered_ports.txt || true
          
          # Satır sonlarını temizle ve mükerrerleri silerek sırala
          sed -i 's/\r//g' filtered_ports.txt
          sort -u filtered_ports.txt > toplanan_linkler.txt
          
          # Geçici dosyaları uçur
          rm -f single_raw.txt single_decoded.txt temp_live.txt filtered_proto.txt filtered_ports.txt

      - name: Commit and Push VIP Port Links
        run: |
          git config --global user.name "VordrBot"
          git config --global user.email "bot@vordr.com"
          git add toplanan_linkler.txt
          
          if [ -s toplanan_linkler.txt ]; then
            git commit -m "Sadece 443 ve 80 Portlu taze linkler yuklendi: $(date +'%H:%M:%S')"
            git push
          fi
