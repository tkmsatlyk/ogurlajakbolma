name: Otomatik Happ Link ToplayÄ±cÄ±

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'KODLARY'
      - 'CONFIG'
      - 'Toplanan_linkler.txt'
      - 'KODLARY_V2'

concurrency:
  group: update-links
  cancel-in-progress: true  # Yeni bir tetikleme gelirse, eskisi Ã§alÄ±ÅŸÄ±yorsa iptal edilir

permissions:
  contents: write

jobs:
  update-links:
    runs-on: ubuntu-latest
    steps:
      - name: Repoyu Kopyala (Checkout)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1   # Sadece son commit'i Ã§ek, tÃ¼m geÃ§miÅŸi deÄŸil -> daha hÄ±zlÄ±

      - name: Python Kurulumu
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'   # pip paketlerini Ã¶nbelleÄŸe alÄ±r, tekrar indirmeyi Ã¶nler

      - name: KÃ¼tÃ¼phaneleri YÃ¼kle
        run: |
          pip install --upgrade pip
          pip install requests

      - name: Scripti Ã‡alÄ±ÅŸtÄ±r
        run: python update.py

      - name: DeÄŸiÅŸiklikleri Kaydet ve GitHub'a GÃ¶nder
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          git diff --quiet && git diff --staged --quiet || (git commit -m "Otomatik gÃ¼ncellendi [Skip CI]" && git push)
